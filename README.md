# AutoClicker🖱️

A simple auto-clicker application built with Java Swing.

## Features

- Configurable click intervals (hours, minutes, seconds, milliseconds)
- Mouse button selection (left, right, middle)
- Customizable hotkeys for start and stop
- Global hotkey support

## Build and Run

```bash
javac -d bin src/main/java/autoclicker/*.java
java -cp bin autoclicker.AutoClicker
